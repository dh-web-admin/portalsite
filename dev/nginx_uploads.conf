# Nginx snippet to serve /uploads from the mounted directory
# Include this in your server block or copy into nginx config

location /uploads/ {
    alias /portalsite/uploads/;
    access_log off;
    expires max;
    add_header Cache-Control "public";
}
